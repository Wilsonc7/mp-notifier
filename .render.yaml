services:
  - type: web
    name: mp-notifier
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: ACCESS_TOKEN
        sync: false
